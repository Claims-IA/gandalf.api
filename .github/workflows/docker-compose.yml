name: Docker Compose

on:
  push:
    branches:
      - "**"
  pull_request: {}

jobs:
  compose-up:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and start containers
        run: docker compose up -d --build

      - name: Show container status
        run: docker compose ps

      - name: Verify nginx responds
        run: |
          for attempt in 1 2 3 4 5; do
            status=$(curl -s -o /tmp/response -w "%{http_code}" http://localhost || true)
            if [ "$status" != "000" ]; then
              echo "Received HTTP status: $status"
              exit 0
            fi
            echo "Waiting for nginx to respond (attempt $attempt)..."
            sleep 2
          done
          echo "No response from nginx."
          exit 1

      - name: Tear down containers
        if: always()
        run: docker compose down -v
